#!/bin/bash

# Compatibility entry point for callers outside Omarchy's graphical PATH.
# The session itself launches the authoritative wrapper from /usr/bin.

set -u

exec /usr/bin/omarchy-hyprland-monitor-watch "$@"
